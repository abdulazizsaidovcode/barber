import DefaultLayout from '../../../layout/DefaultLayout';
import NotFound from './detail';

export const FirstTab = () => {
  return (
    <DefaultLayout>
      <NotFound />
    </DefaultLayout>
  );
};
